# Pinned so that a nightly job and a pull request plan always agree.
# Bump with a pull request, never in place.
octodns==1.22.0
octodns-cloudflare==1.2.0

# Used by tools/merge_live.py to merge Cloudflare state back into the zone
# files without destroying the ownership comments.
ruamel.yaml==0.19.1

# Imported directly by tools/merge_live.py so that it sorts records exactly
# the way octoDNS's enforce_order checks them. It arrives with octodns anyway,
# but a direct import deserves a direct pin.
natsort==8.4.0
